import { Stack } from 'expo-router';
import React from 'react';

function Nav() {
  return <Stack />;
}

export default Nav;

// This file is used to define the navigation structure of the app.
// It uses the expo-router library to create a stack navigation.
// The Stack component is used to define the navigation stack.
